- link:
    ~/.zshrc:
      path: .zshrc
      relink: true
    ~/.zshenv:
      path: .zshenv
      relink: true

- shell:
    - command: echo "✅ .zshenv .zshrc done"
      description: "Confirming setup completion"
      stdout: true
